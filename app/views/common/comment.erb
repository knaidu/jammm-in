<% delete_js_callback ||= '' %>

<div class='float-left'>
	<%= add_profile_link(comment.user) %>: 
</div>
<% if (managers + [comment.user]).include?(session_user?) %>
	<span class='float-right simple-delete simple-link' onclick="deleteComment('<%= comment.id %>', '<%= comments_div_id %>', '<%= form_id %>')" title='Delete comment'>x</span>
<% end %>
&nbsp;<%= comment.comment %>
<% if @session_user == comment.user %>
<% end %>