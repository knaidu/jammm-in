<%
  icons = {
    :user_follows => :following,
    :jam_created => :jam,
    :jam_published => :jam,
    :song_created => :song,
    :song_published => :song,
    :user_created => :user
  }

%>

<% feeds.each {|feed| %>
	<%= partial("common/feed", :locals => {:feed => feed, :show_as_updates => show_as_updates}) %>
  <%= vspace %>
<% } %>
