<div class='menu'>
	<span class='profile-photo'>
	  <%= profile_picture(@user) %>
	</span>
	
	<br><br>
	<%= partial("profile/actions", :locals => {:user => @user}) %>
	<br><br>

	<%
	  items = [
	    {:text => 'Profile', :link => "/#{@user.username}", :icon => :profile},
	    {:text => 'Songs', :link => "/#{@user.username}/songs", :icon => :song},
	    {:text => 'Jams', :link => "/#{@user.username}/jams", :icon => :jam},
	    {:text => 'Following', :link => "/#{@user.username}/following", :icon => :following},
	    {:text => 'Jammmed With', :link => "/#{@user.username}/jammmed_with", :icon => :collaborators},
	    {:text => 'Followers', :link => "/#{@user.username}/followers", :icon => :followers}    
	  ]
  
	  %>
	  <div>
	    <hr class='thin'>
			<%= partial("common/menu", :locals => {:items => items}) %>
	  </div>
	  <%
	%>

	<br><br>
	<%= section_header('Following', {:icon => :following}) %>
	<%= partial(:'common/list_artists', :locals => {:artists => @user.follows}) %>
	<br><br>

	<%= section_header('Collaborators', {:icon => :collaborators })%>
	<%= partial(:'common/list_artists', :locals => {:artists => @user.collaborators}) %>

	<br><br>
	<%= section_header('Followers', {:icon => :followers}) %>
	<%= partial(:'common/list_artists', :locals => {:artists => @user.followed_by}) %>
</div>
