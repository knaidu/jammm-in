<% 
  if session_user? and @user != session_user? 
  message_stream = MessageStream.find_stream(session_user?, @user) %>
  <%= partial("common/message_stream", 
    :locals => {:message_stream => message_stream}, 
    :section_header => SectionHeader.new('Message Stream', {:img => :message}), 
    :collapsible => true,
    :collapsed => true
  ) %>
<% end %>

<%= section_header('Songs', {:icon => :song}) %>
<%= partial (:'common/songs', :locals => {:songs => @user.songs}) %> 


<%= section_header('Jams', {:icon => :jam}) %>
<%= partial (:'common/jams', :locals => {:jams => @user.jams}) %> 
