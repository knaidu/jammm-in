<% 
	song = fhlp_get_song(feed) 
	users = fhlp_get_users(feed)
%>

<%= list_artists (users) %> <%= (users.size > 1) ? "have" :"has" %> published the song <%= song_link song %>.
<%= vspace 2 %>
<%= play song %>