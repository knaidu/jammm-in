<%= add_link 'Invite'%> <br><br>
<% artists.each do |artist| %>
  <%= partial('song/manage/artist', {:locals => {:artist => artist }}) %>
<% end %>
<%= "(<i>There are no artists added</i>)" if artists.empty? %>
<br>
