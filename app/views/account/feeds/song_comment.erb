<% 
  song = feed.dataObj.song 
  comment = feed.dataObj.comment
%>
<div class='feed' containsonhoveritems=true>
  <div class='thumbnail'>
    <img src='<%= comment.user.profile_picture_url %>'>
  </div>
  <div class='content'>
    <div class='bold link clickable' <%= profile_link comment.user %>><%= comment.user.username %></div><%= vspace %>
    <%= feed.dataObj.comment.comment %>
    <div class='entity'>
      <div class='entity-image'>
        <img src='<%= song.song_picture_url %>'>
      </div>
      <div class='entity-info'>
        <div class='bold link' <%= music_link song %>><%= song.name %></div>
        <%= vspace 10 %>
        <div>
          <div class='field-name'>Views:</div><%= song.views %>
          <%= hspace 10 %>
          <div class='field-name'>Length:</div><%= song.file_data.length.to_time_str rescue "-" %>
          <%= hspace 10 %>
          <div class='field-name'>Likes:</div><%= song.liked_by.count %>
        </div>
      </div>
    </div>
    <div class='time'><%= feed.created_at.reference_string %>&nbsp;
      <div class="float-right"><%= render_mini_play_text(song) %></div>
    </div>
  </div>
</div>