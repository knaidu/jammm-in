<%
	default_color = "#990000"
	default_dark_color = "#800000"
	default_round = "-moz-border-radius: 5px; -webkit-border-radius: 5px; border-radius: 5px;"
	light_gray  = "#eee9e9"
	font_family = "Arial,Helvetica,sans-serif"
	left_panel_width = "200px";
	right_panel_width = "200px";
%>

/* DEFAULTS */

.default-link-color {color: #990000;}


/* HTML, BODY */
html, body {
  margin:0; 
  padding:0; 
  width:100%; 
  height:100%; 
  overflow: auto;
  font-family: <%= font_family %>; 
  font-size: 12px;
}

/* GENERAL */
.clear-both {clear: both}

IMG {border: none}

.top-bar {
  height: 60px;
  background-color: <%= default_color %>;
  color: white;
  position: fixed;
  width: 100%;
  z-index: 3;
}

.top-bar .logo {position: absolute; width: <%= left_panel_width %>; height: 100%}
.top-bar .search {position: absolute; right: 0px; width: <%= right_panel_width %>; height: 100%; padding: 18px;}
.top-bar .search INPUT {width: 100%}
.top-bar .menu {color: white; margin-left: <%= left_panel_width %>; font-size: 22px; padding: 5px 5px 5px 20px}
.top-bar .menu .item {display: inline-block; margin-right: 30px; background-color: red; cursor: pointer}
.top-bar .menu .item .text {padding: 10px}

.middle-bar {
  margin-top: 60px;
  position: fixed;
  overflow: auto;
}

.middle-bar .menu-panel {position: fixed; width: <%= left_panel_width %>; border-right: 1px solid grey; z-index: 2; background-color: white;}
.middle-bar .content-panel {position: relative; margin-left: 200px; z-index: 1; padding-left: 20px}

.bottom-bar {position: fixed; background-color: #3D3D3D; bottom: 0px; width: 100%; height: 20px}

.menu-panel .navigation-bar {width: 100%; height: 50px; border-bottom: 1px solid grey;}
.menu-panel .navigation-bar .icon {position: absolute; width: 32px; margin-top: 10px}
.menu-panel .navigation-bar .content {position: relative; margin-left: 32px; margin-top: 5px}
.menu-panel .navigation-bar .content .name {font-weight: bold;}

.dialog {z-index: 10; display: none; min-width: 400px; height: 200px; position: fixed; background-color: yellow; border: 1px solid grey; left: 100px; top: 10px}

.jams {position: relative; clear: both;}
.jams .jam {position: relative; float: left; height: 70px; width: 200px; overflow: hidden;}
.jams .jam .icon {position: absolute; width: 64px; height: 64px}
.jams .jam .content {position: relative; margin-left: 70px; overflow: hidden; }
.jams .jam .content .name {overflow: hidden; font-weight: bold;}